{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
<h1>Reponse</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ reponse.id }}</td>
        </tr>
        <tr>
            <th>Reponse</th>
            <td>{{ reponse.reponse }}</td>
        </tr>
    </tbody>
</table>
<div class="my-2">
    <a class="btn btn-primary" href="{{ path('app_reponse_index') }}">back to list</a>
    <a class="btn btn-primary" href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>
</div>
{{ include('reponse/_delete_form.html.twig') }}
{% endblock %}